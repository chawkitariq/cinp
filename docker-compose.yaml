services:
  db:
    image: "postgres:18.4"
    volumes:
      - dbdata:/var/lib/postgresql
    environment:
      POSTGRES_USER: cinp
      POSTGRES_PASSWORD: cinp
      POSTGRES_DB: cinp
    ports:
      - 5432:5432

volumes:
  dbdata:
